// Plugin JavaScript
console.log('Plugin loaded: panel');

export default {
    /**
     * The Vue entry (`app.js`) registers no global components, so there is nothing to register here.
     */
    register(): void {
        // no-op
    },
};
